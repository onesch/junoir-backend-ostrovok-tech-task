services:
  postgres:
    image: postgres:16
    container_name: ostrovok-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ostrovok
    ports:
      - "5433:5432"
    volumes:
      - ./database/dump.sql:/docker-entrypoint-initdb.d/dump.sql
